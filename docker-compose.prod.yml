services:
  api:
    build:
      context: ./api-node
      dockerfile: docker/Dockerfile
    environment:
      NODE_ENV: production

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:80"

  ml-classifier:
    build:
      context: ./ml-classifier-python
      dockerfile: Dockerfile
